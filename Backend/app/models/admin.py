from db import Base, get_db, Session
from sqlalchemy.orm import relationship
from sqlalchemy import Column, Text, TIMESTAMP



class BaseAdmin(Base):
    id = Column(Text, primary_key=True)
    name = Column(Text)
    name = Column(Text)
    name = Column(Text)
    name = Column(Text)
    name = Column(Text)
    name = Column(Text)
    name = Column(Text)
    name = Column(Text)
    name = Column(Text)
